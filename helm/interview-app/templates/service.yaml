apiVersion: v1
kind: Service
metadata:
  name: interview-app-service

spec:
  selector:
    app: interviewapp

  type: {{ .Values.service.type }}

  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8083